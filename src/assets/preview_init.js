// preview_init.js — injected into webview pages
// Installed when wry feature is enabled
(function() {
  // Empty init script — extend as needed
  console.log('[Termia] Web preview initialized');
})();
